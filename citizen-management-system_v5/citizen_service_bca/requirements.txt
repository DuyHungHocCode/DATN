# FastAPI framework and ASGI server
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# Pydantic for data validation and settings management
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Database (PostgreSQL) interaction
sqlalchemy[asyncio]>=2.0.0,<3.0.0  # ORM, include asyncio extras for async DB access
psycopg2-binary>=2.9.0,<3.0.0      # PostgreSQL adapter (binary version is often easier for install)
# Alternatively, use asyncpg for fully async DB driver:
# asyncpg>=0.29.0,<1.0.0

# Kafka integration (Choose one library)
kafka-python>=2.0.0,<3.0.0        # Popular Kafka client library
# OR confluent-kafka>=2.0.0,<3.0.0 # Often recommended for performance/features

# HTTP Client (for calling BTP service if needed, although BCA is primarily called)
httpx>=0.26.0,<1.0.0

# Security: Password hashing and JWT
passlib[bcrypt]>=1.7.0,<2.0.0
python-jose[cryptography]>=3.3.0,<4.0.0

# Environment variable loading
python-dotenv>=1.0.0,<2.0.0

